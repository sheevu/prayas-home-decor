import React from 'react'
import ReactDOM from 'react-dom/client'
import App from '../prayas_home_decor_digital_brochure.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
